{
  "schema": "migration-parity/parity-input@1",
  "cases": [
    {
      "case_id": "coverage-mcp.transport.contract.schema7",
      "surface": "coverage-mcp.transport",
      "operation": "contract",
      "covers": ["coverage-mcp.transport.contract.schema7"],
      "target_profiles": ["rust-default"],
      "assets": [],
      "steps": [
        {
          "step_id": "contract",
          "surface": "coverage-mcp.transport",
          "operation": "contract",
          "receiver": null,
          "arguments": {"input": {"kind": "literal", "value": {"schema_revision": 7, "tools": 11, "resources": 2}}}
        }
      ],
      "observations": ["contract"]
    }
  ]
}
